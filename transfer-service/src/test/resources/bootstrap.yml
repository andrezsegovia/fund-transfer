eureka:
  client:
    enabled: false
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
spring:
  application:
    name: transfer-service
  cloud:
    config:
      discovery:
        enabled: false
        serviceId: CONFIGSERVER
      failFast: false